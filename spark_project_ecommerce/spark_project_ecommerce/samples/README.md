These files are **small schema examples** for reviewers and for local smoke tests. They are **not** the full incremental dataset.

Expected landing-style paths in your ADLS / volume layout should mirror how `0_data/` is organized locally (see main project README).

Files:

- `order_items_sample.csv` — line-level orders
- `shipments_sample.csv` — shipment facts
- `returns_sample.csv` — return events
